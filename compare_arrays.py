# list_one = [1, 2, 5, 6, 2]
# list_two = [1, 2, 5, 6, 2]

# same_values = set (list_one) & set (list_two)

# print same_values, 'The list are the same'



list1 = [1,2,5,6,5]
list2 = [1,2,5,6,5,3]


def find_identical(list):

	if list1 == false
		retun 

print 'The list are not the same' 


# same_values = set (list1) & set (list2)

# print same_values, 'The list are not the same'


# lista = [1,2,5,6,5,16]
# listb = [1,2,5,6,5]

# same_values = set (lista) & set (listb)

# print same_values, 'The list are not the same'



# listc = ['celery','carrots','bread','milk']
# listd = ['celery','carrots','bread','cream']

# same_values = set (listc) & set (listd) 

# print same_values, 'The list are not the same